# Cloudflare Pages configuration
# This file is used for local development with wrangler
# Actual deployment is handled by Cloudflare Pages dashboard

name = "gately-help-center"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

# Worker configuration for domain routing
[[workers]]
name = "gately-domain-router"
main = "worker-router.js"
compatibility_date = "2024-01-01"
